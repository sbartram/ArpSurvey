# Copy to values-secret.yaml and fill in real values.
# values-secret.yaml is gitignored — NEVER commit it.
# Usage: helm upgrade --install arp-survey charts/arp-survey -f charts/arp-survey/values-secret.yaml
secret:
  databaseUrl: "postgresql://arp:CHANGEME@postgresql.postgres.svc.cluster.local:5432/arpsurvey"
  flaskSecretKey: "CHANGEME-generate-with-openssl-rand-hex-32"
